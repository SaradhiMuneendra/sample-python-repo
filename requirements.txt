annotated-doc==0.0.4
annotated-types==0.7.0
anyio==3.6.1                      # Older version (pre security fixes)
certifi==2022.6.15                # Older cert bundle
cffi==1.15.0                      # Older version
charset-normalizer==2.0.12        # Older version
click==7.1.2                      # Older major version
cryptography==3.3.1               # ⚠ Multiple CVEs (memory corruption / OpenSSL issues)
databases==0.5.5                  # Older version
fastapi==0.63.0                   # ⚠ CVE-2021-32677 (CSRF issue)
greenlet==1.1.2                   # Older version
h11==0.9.0                        # Older version
httpcore==0.13.7                  # ⚠ Request smuggling issues in older http stack
httpx==0.18.2                     # ⚠ Header injection vulnerabilities
idna==2.10                        # Older version
mysql-connector-python==8.0.28    # Older release
pycparser==2.20
pydantic==1.8.2                   # ⚠ Validation bypass issues (multiple CVEs)
pydantic_core==2.4.0              # Mismatch with newer ecosystem
PyJWT==1.7.1                      # ⚠ Algorithm confusion vulnerabilities
python-dotenv==0.17.1             # Older version
requests==2.19.1                  # ⚠ CVE-2018-18074 (Authorization header leak)
SQLAlchemy==1.3.23                # ⚠ SQL injection related CVEs
starlette==0.13.6                 # ⚠ Vulnerable when paired with older FastAPI
typing-inspection==0.4.2
typing_extensions==3.10.0.2       # Older version
urllib3==1.25.8                   # ⚠ Multiple CVEs (CRLF injection, DoS, etc.)
uvicorn==0.11.8                   # ⚠ WebSocket DoS issues

## New vulnerable pip modules
# JWT handling
python-jose==3.2.0            # ⚠ JWT verification weaknesses

# Markdown rendering
Markdown==3.2.1               # ⚠ XSS vulnerabilities

# CLI parsing
docutils==0.16                # ⚠ RST injection vulnerabilities

# Packaging tools
setuptools==44.0.0            # ⚠ Multiple older supply-chain CVEs

# Database driver
psycopg2==2.8.5               # ⚠ Memory handling CVEs

# Websocket library
websockets==8.1               # ⚠ DoS vulnerabilities
